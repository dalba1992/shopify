<div class="holder mt-0">
  <div class="container">
    <div class="page-title text-center">
        <div class="title">
            <h1>{{ page.title }}</h1>
        </div>
    </div>
    {{ page.content }}
  </div>
</div>